To install plugin, download Section Maker folder.<br>
In Figma, hit `CMND+/` and navigate to the `Plugins & widgets` tab.<br>
Click on `Import from manifest...` and select the `manifest.json` file.<br>
Run the plugin.
